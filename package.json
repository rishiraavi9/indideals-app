{
  "name": "deals-app",
  "version": "1.0.0",
  "description": "Deals aggregation platform for India market",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build:all": "cd backend && npm run build && cd ../frontend && npm run build",
    "setup": "npm run install:all && cd backend && npm run db:generate && npm run db:migrate && tsx src/seed.ts"
  },
  "keywords": [
    "deals",
    "ecommerce",
    "india",
    "slickdeals"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "@sentry/node": "^10.30.0",
    "@types/nodemailer": "^7.0.4",
    "dompurify": "^3.3.1",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "ioredis": "^5.8.2",
    "isomorphic-dompurify": "^2.34.0",
    "nodemailer": "^7.0.11",
    "rate-limit-redis": "^4.3.1",
    "winston": "^3.19.0"
  }
}
